# @deepsickdev/core

Shared setup and configuration packages for the `@deepsickdev` scope.

Right now this package exposes a reusable **Biome** configuration that you can extend in any project.

---

## What you get

- `@deepsickdev/core/biome` → shared `biome.json` config (exported from this package)

---

## Installation

### npm
```bash
    npm i -D @deepsickdev/core @biomejs/biome
